{
	"name": "docker-proxy",
	"version": "1.0.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"wrangler": "wrangler dev",
		"deno": "deno run --allow-net --allow-env src/deno.ts"
	},
	"devDependencies": {
		"wrangler": "^4.107.0",
		"deno": "^2.8.3"
	},
	"overrides": {
		"esbuild": "^0.28.1"
	}
}
